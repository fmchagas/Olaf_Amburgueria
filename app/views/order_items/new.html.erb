<div class="page-header">
  <%= link_to order_order_items_path(@order), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Voltar
  <% end %>
  <h1>Novo order_item</h1>
</div>

<%= render 'form' %>
